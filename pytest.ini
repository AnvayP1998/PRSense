[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
markers =
    regression: live-LLM regression suite (needs GEMINI_API_KEY/GROQ_API_KEY and data/regression_cases.json)
addopts = -m "not regression"
