# ── Phase 1: Foundation ───────────────────────────────────────────────
# All entries are open-source libraries (MIT/Apache/BSD) — no paid services.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
PyGithub>=2.5
httpx>=0.27

# ── Dev / test ────────────────────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24

# ── Phases 2-5 dependencies are added as each phase lands ──────────────
# langgraph, langchain, langchain-google-genai, langchain-groq,
# mcp, chromadb, supabase, langsmith, streamlit, pandas
