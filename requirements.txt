# ── Phase 1: Foundation ───────────────────────────────────────────────
# All entries are open-source libraries (MIT/Apache/BSD) — no paid services.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
PyGithub>=2.5
httpx>=0.27

# ── Phase 2: MCP layer + RAG (all OSS, no paid services) ──────────────
mcp>=1.29                 # official Model Context Protocol SDK (MIT)
chromadb>=1.5             # local vector store, ONNX MiniLM embeddings, offline (Apache-2.0)
langgraph>=1.2            # agent state machine (MIT)
langchain-core>=1.6       # tool/message primitives (MIT)
langchain-mcp-adapters>=0.3  # exposes MCP tools to LangChain/LangGraph (MIT)

# ── Phase 3: LangGraph review agent + LLMs + tracing ───────────────────
langchain-google-genai>=2.1   # Gemini, free tier (MIT)
langchain-groq>=0.2           # Groq fallback, free tier (MIT)
langsmith>=0.12                # tracing client, free tier (MIT)

# ── Phase 4: evaluation framework ───────────────────────────────────────
# (dataset storage is JSONL + stdlib sqlite3 — no new dependency needed)

# ── Dev / test ────────────────────────────────────────────────────────
pytest>=8.3
pytest-asyncio>=0.24

# ── Phase 5 dependencies are added as that phase lands ─────────────────
# supabase, streamlit, pandas
